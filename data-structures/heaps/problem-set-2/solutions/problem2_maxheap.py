# DEPRECATED: Functionality moved to heaps.py (shared reusable module)
# This module is kept for reference but is no longer the canonical implementation.
# Use naive.py or optimized.py which import from heaps.py instead.
