class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


def is_balanced(root: TreeNode) -> bool:
    """
    Checks if a Binary Search Tree is balanced.
    A tree is balanced if for each vertex, the size of the left subtree
    differs from the size of the right subtree by at most 1.
    """
    # TODO: Implement this using the efficient O(n) recursive approach
    pass


def countNodes(root: TreeNode) -> int:
    """
    Helper function to count the number of nodes in a given tree.
    """
    # TODO: Implement the helper function
    pass


def kthSmallest(root: TreeNode, k: int) -> int:
    """
    Finds the k-th smallest element in a Binary Search Tree.
    """
    # TODO: Implement this using the efficient approach
    pass
