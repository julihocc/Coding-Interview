"""
TEMPLATE: Find Crossover Indices Solution

Function Signature:
    def findCrossoverIndex(x, y):

Problem:
    Find index i where x[i] >= y[i] and x[i+1] < y[i+1]
    (both x and y are sorted arrays)

Reference: See ../README.md for full problem description
"""


def findCrossoverIndex(x, y):
    """
    [FILL IN: Brief one-liner describing your approach]
    
    APPROACH:
    [Describe your strategy here - e.g., Linear Scan, Binary Search, etc.]
    
    Time Complexity: O(?)
    Space Complexity: O(?)
    """
    
    # STEP 1: Input validation
    assert len(x) == len(y), "Arrays must have equal length"
    n = len(x)
    
    # STEP 2: Handle edge cases if any
    # [Add edge case handling if needed]
    
    # STEP 3: Main algorithm
    # [Implement your solution here]
    pass
    
    # STEP 4: Return result
    # [return result]
