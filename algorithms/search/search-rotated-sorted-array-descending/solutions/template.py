"""TEMPLATE: Class-based solution for Search Rotated Sorted Array Descending

Reference: See ../README.md for full problem description
"""

class Solution:
    def search(self, nums: list[int], target: int) -> int:
        """Search for a target value in a rotated sorted array (descending).

        Args:
            nums: A list of integers sorted in descending order (with distinct values)
                  that is rotated at some pivot unknown to you beforehand.
            target: The integer value to search for.

        Returns:
            The index of target if it is in nums, or -1 if it is not in nums.
        """
        # TODO: Implement your solution here
        return -1
